apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: neuralnetjs

resources:
  - namespace.yaml
  - deployment.yaml
  - ingress.yaml
  - hpa.yaml

commonLabels:
  app: neuralnetjs
  managed-by: kustomize
